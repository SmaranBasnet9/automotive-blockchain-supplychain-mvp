// React entry point for the automotive blockchain supply chain MVP.
// This file renders the App component into the root element.

import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
